{
	"name": "lyric-generator",
	"version": "0.1.0",
	"description": "Generate song lyrics by vibe — offline by default, Claude-enhanced if you have a key.",
	"main": "app.js",
	"type": "module",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"start": "node app.js",
		"dev": "node --watch app.js",
		"test": "node --test test/*.test.js"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.30.1",
		"dotenv": "^16.4.5",
		"express": "^4.19.2"
	}
}
